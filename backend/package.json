{
  "name": "veezo-backend",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node -e \"console.log('Backend has no compile step')\"",
    "dev": "node server.js",
    "start": "node server.js",
    "seed": "node -e \"require('dotenv').config(); const {connectDB}=require('./db/connection'); const {seedDatabase}=require('./db/seed'); connectDB().then(seedDatabase).then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\"",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.7.0",
    "cors": "^2.8.0",
    "dotenv": "^16.4.1",
    "express": "^4.21.1",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.0",
    "morgan": "^1.10.1",
    "multer": "^2.1.1",
    "nodemailer": "^6.9.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0"
  }
}
